# Scripts

This directory contains the public research code used to prepare the multi-climate experiment, train and freeze the PPO ensemble, execute the held-out controller evaluation, perform the statistical and diagnostic analyses, and generate the publication figures.

Run scripts from the repository root, for example:

```bash
python scripts/evaluate_all_controllers.py
```

The scripts use paths relative to the repository root so that protocol and result files remain in their documented locations.
